// ============================================================
// Mercotrace — Accounting data is backend-only.
// ============================================================
// Ledgers: chartOfAccountsApi (GET /api/chart-of-accounts).
// Vouchers: voucherHeadersApi (GET/POST /api/voucher-headers).
// No seed or mock data; no localStorage.

export function initializeAccountingData() {
  // No-op. All accounting data comes from backend APIs.
}
